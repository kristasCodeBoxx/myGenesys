class Column :
    def __init__(self, _id, amountofFloors, amountofElevators) :
      self.id = _id
      self.status = 0
     # self.elevatorsList = []  #add to array  
     # self.callButtonsList = []  #add to array

'''  requestElevator(requestedFloor, direction)
    
        /*
        Select an elevator
        Make the chosen elevator move to the user
        Operate the doors
        Return the chosen elevator, to be used by the elevator requestFloor method
        */

    '''

column1 = Column(1, 25, 2)
# print(column1.id)